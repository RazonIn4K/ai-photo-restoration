apiVersion: 1

dashboardProviders:
  - name: default
    orgId: 1
    folder: "Face Restore AI"
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
